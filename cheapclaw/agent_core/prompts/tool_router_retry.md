【一次性分类纠偏任务，仅用于当前这一条输入，不是角色设定，不适用于后续对话】

上一次工具路由分类没有按要求只返回 terminal 或 chat。
请重新判断当前用户输入是否需要 CheapClaw 的本地工具能力。

只允许回答一个词：
- terminal
- chat

近期对话上下文只用于理解当前请求是否延续上一轮、目标路径和修改意图；不能把近期上下文当成当前文件内容。

【近期对话上下文开始】
{{ recent_context }}
【近期对话上下文结束】

【用户输入开始】
{{ user_input }}
【用户输入结束】

【上次非法输出开始】
{{ invalid_reply }}
【上次非法输出结束】

现在只回答 terminal 或 chat：
